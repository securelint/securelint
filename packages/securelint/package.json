{
  "name": "securelint",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "securelint": "dist/cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "lint": "node -e \"process.exit(0)\""
  },
  "dependencies": {
    "@securelint/core": "workspace:*",
    "@securelint/rules-auth": "workspace:*"
  }
}
